import { FC } from "react";
import useInitCashflowState from "../hooks/UseInitCashflowState";

const CashflowTemplate: FC = () => {
  useInitCashflowState();

  return <></>;
};

export default CashflowTemplate;
